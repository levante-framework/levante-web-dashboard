.vercel
node_modules/
data/geonames/
data/geonames_cities5000.zip
.env

data/tiger/
.env*.local
data/gallery/weather-cache.json

# Audio validation result files are generated locally (or via sibling repos) and should not ship to Vercel by default.
data/validation/*.json
data/validation/surveys-embedding-model-*.csv
data/validation/surveys-embedding-model-*.pdf
data/validation/embedding-model-compare-*.csv
data/validation/embedding-model-compare-*.json
data/validation/embedding-model-compare-*.pdf

public/locations-map.html

# Python virtual environment (should never be committed)
venv/
venv-emb/
.venv/
.venv-emb/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# WorldPop raster files (large binary files, download as needed)
data/population/worldpop/*.tif
data/population/worldpop/*.tiff

# WorldPop cache (generated locally)
data/gallery/worldpop-cache.json

# GeoBoundaries data files (stored in GCS bucket, not in repo)
data/geoboundaries/**/*.json.gz
data/geoboundaries/**/*.json
data/geoboundaries/**/*.tmp

# Geofabrik PBF cache (large files, can be re-downloaded)
data/geofabrik/**/*.osm.pbf
data/geofabrik/**/*.tmp

# Geofabrik boundary packs (stored in GCS bucket, not in repo)
public/adm-packs/**/*geofabrik*.json.gz

# US ADM6-10 state-specific packs (stored in GCS bucket, not in repo)
public/adm-packs/us/adm6/
public/adm-packs/us/adm7/
public/adm-packs/us/adm8/
public/adm-packs/us/adm9/
public/adm-packs/us/adm10/
node_modules
node_modules
